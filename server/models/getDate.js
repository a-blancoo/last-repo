/* 
function getDate() {
    const date = new Date();
    const beatyDate = [date.getTime(), '-', date.getDate(), '-', date.getMonth() + 1, '-', date.getFullYear()];
    return beatyDate;
}
export default getDate
 */